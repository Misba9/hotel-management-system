rules_version = '2';
// TEMPORARY: open Storage for local debugging (CORS / upload). Revert to scoped rules before production.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
