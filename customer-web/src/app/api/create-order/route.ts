/**
 * Canonical alias for Razorpay server order creation.
 * Implementation lives in payments/razorpay (server-side pricing + auth).
 */
export { dynamic, POST } from "../payments/razorpay/create-order/route";
