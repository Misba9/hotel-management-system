/**
 * Native-only Firebase Auth entry — always use the RN build bundled with `firebase@10.x`.
 * Metro maps `firebase/auth` → this file on android/ios (see metro.config.js).
 */
module.exports = require("customer-mobile-firebase-auth-rn");
