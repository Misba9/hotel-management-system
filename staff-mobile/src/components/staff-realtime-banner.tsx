/**
 * Legacy role-change toast (was tied to `StaffAuthProvider`). Expo Router + Zustand handle session elsewhere.
 * Reserved for a future store-driven banner if needed.
 */
export function StaffRealtimeBanner() {
  return null;
}
